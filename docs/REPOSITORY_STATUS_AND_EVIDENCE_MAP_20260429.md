# Repository Status and Evidence Map (2026-04-29)

**Status: diagnostic (repository state map; not manuscript claim text).**

## Branch/recent-state audit
- Current branch: `work`.
- Latest local commit at audit start: `56e84c1b6633b38df578ddb14d2e621a078c93f0`.
- Compact ledger tracking path is git-tracked: `outputs/cohere_compact_ledgers/`.
- Recent merged PRs from local history relevant to Cohere diagnostics/chunking/status docs:
  - PR #307 (`56e84c1`): harden Codex-local Cohere chunking scripts.
  - PR #306 (`0b2c13b`): internal accuracy variant audit/preflight.
  - PR #305 (`db658b5`): internal methods audit for accuracy comparison.
  - PR #304 (`403bdeb`): Cohere validation and reporting package.

## Evidence hierarchy (authoritative ordering)
1. **Canonical (claim-eligible)**
   - Generated by `scripts/paper/run_all_neurips_paper_artifacts.py`.
   - Outputs: `outputs/paper_tables/`, `outputs/paper_plot_data/`, `outputs/paper_figures/`.
   - Claims only through `docs/PAPER_SOURCE_OF_TRUTH.md`.
2. **Diagnostic real-model (supporting only)**
   - Cohere/OpenAI runs, Codex-local chunks, compact ledgers, preflights.
3. **Partial/provenance-only**
   - Tiny checks, interrupted runs, launch-attempt notes, Wulver/Slurm handoffs.
4. **Excluded diagnostic-only methods**
   - `direct_reserve_semantic_frontier_v2_thresholded_ordered` is excluded from live runtime comparisons.

## Durable Cohere progress source
- Durable Codex-local source of truth:
  - `outputs/cohere_compact_ledgers/20260429T_COHERE_FULL_ACCURACY_CODEX_compact_per_example_ledger.csv`
- Markdown-only progress logs are historical unless backed by compact-ledger rows.
- Current `openai/gsm8k`, budget `2`, seed `11`, 9-method slice status from durable ledger-backed status flow: **incomplete**.

## Workflow policy
- Keep raw run directories under `outputs/cohere_real_model_cost_normalized_validation_*` as non-durable unless compact ledger export is created.
- For Codex-local continuation, use status → missing chunk run → compact ledger export → status → aggregate.
