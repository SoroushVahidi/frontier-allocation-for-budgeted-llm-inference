# Figure 1 Schematic Notes

Source file:
- `outputs/paper_plot_data/figure1_problem_schematic.json`

Purpose:
- Provide a manuscript-facing conceptual schematic of the current project identity:
  input questions -> active branches/controllers -> budget allocation decisions -> commit/final selection.

Design rules:
- Keep neutral, factual labels.
- Avoid old binary revise-routing semantics.
- Keep one directional pipeline to match paper narrative.

Manual rendering guidance:
- Use six boxes in left-to-right flow.
- Place annotations under the pipeline.
- Keep naming exactly aligned with the JSON labels.
