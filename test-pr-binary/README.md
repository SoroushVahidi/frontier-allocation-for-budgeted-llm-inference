# PR Behavior Test

This directory exists solely to verify that Copilot can successfully open a pull request when a binary file is included in the changeset.

- `dummy.bin` — an 8-byte dummy binary file with no functional purpose.

**Do not merge this PR into the main branch.** It may be closed once the test is confirmed.
