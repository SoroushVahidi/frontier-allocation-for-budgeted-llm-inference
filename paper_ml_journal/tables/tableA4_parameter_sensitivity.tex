\begin{table*}[t]
\centering
\caption{Parameter grounding for FIX-2+FIX-4 and the evaluation protocol. We report parameter grounding and available robustness checks rather than a full continuous hyperparameter sweep; a full threshold sweep is left for future work. All values are fixed and applied identically across all evaluated splits.}
\label{tab:param_sensitivity}
\scriptsize
\begin{tabular}{p{0.14\textwidth}p{0.09\textwidth}p{0.18\textwidth}p{0.18\textwidth}p{0.19\textwidth}p{0.13\textwidth}}
\toprule
Parameter / Design choice & Value used & Role in method / evaluation & Justification & Robustness evidence available & Remaining limitation \\
\midrule
Budget ($B$) & 6 & Hard cap on logical API calls per example; ensures matched-budget comparison & Fixed by provider run protocol; all methods operate under the same $B$ & Sensitivity source (+100 examples) included as separate labeled set; budget value held constant & No multi-$B$ sweep reported \\
\addlinespace
Low-depth guard trigger: \texttt{single\_weak\_frontier\_branch} & String match (implementation constant) & Identifies frontier answers from structurally weak search paths & Derived from failure-trace analysis of frontier run logs; see \texttt{experiments/support\_aware\_selector.py} & Trigger frequency and recovery/regression counts available in postrun artifacts & No threshold variation sweep in manuscript \\
\addlinespace
Low-depth guard trigger: \texttt{frontier\_support}$=0$ $\wedge$ \texttt{candidate\_pool\_answer\_group\_count}$\geq 2$ & Integer threshold (0 and 2) & Detects zero-support frontier outputs when the pool contains multiple distinct answer groups & Conservative condition: requires complete absence of support combined with pool diversity & Condition derived from offline artifact review; trigger counts in package & No continuous threshold sweep \\
\addlinespace
External-consensus condition (FIX-4) & \texttt{l1}=\texttt{s1}=\texttt{tale} $\wedge$ all $\neq$ frontier & Identifies unanimous external agreement diverging from frontier & Unanimity is a strong, conservative signal; partial agreement is not acted on & Trigger frequency available from postrun replay artifacts & No partial-consensus variant evaluated \\
\addlinespace
Bootstrap resamples & $n = 5{,}000$ & Controls Monte Carlo variance in paired bootstrap CI estimation & Standard practice for stable 95\% CI estimation at $n \leq 720$ & Single-seed results show consistent sign agreement; no resample-count sensitivity sweep & Single seed; no multi-seed resample check reported \\
\addlinespace
Evaluation seeds / source split & Seeds 41, 61, 71; sources main300, independent120, final300 & Define the disjoint aggregate and source-stratified analysis & Seeds chosen to span independent run batches; disjointness verified by example ID and question hash & Source-stratified CIs show consistent positive sign for all baselines & Single task/provider; no cross-dataset seed check \\
\addlinespace
Disjointness check & Zero overlap on example IDs and question hashes & Prevents data leakage between primary sources & Verified programmatically on all three sources & Confirmed in postrun package; overlap count = 0 & Only within GSM8K; no cross-dataset overlap check \\
\addlinespace
Provider / model & Cohere command-r-plus-08-2024 & Defines the inference backend for all runs & Chosen for API availability and run stability during the experiment period & All comparisons use the same provider/model; no cross-provider replication & Provider-specific; generalization not established \\
\bottomrule
\end{tabular}
\end{table*}
