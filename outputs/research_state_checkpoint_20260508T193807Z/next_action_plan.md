# Next action plan

1. Prioritize production-equivalent controller integration; if integration risk blocks immediate progress, run a larger non-overlap failure collection first to avoid idle time.
2. Add SC6/PAL into a controlled 25/50-case comparison if call budget allows, maintaining the same case alignment protocol.
3. Cluster unknown-family failures and design the next discovery mechanism only after additional data or production-equivalent comparison evidence.

## What not to do next
- do not integrate `discovery3_candidate_diversity_selection_v1`
- do not rerun 15-case patch immediately
- do not claim best among all methods yet
