BRANCH_FAMILY: backward_from_target_check_live_pilot_v1
MODE: live_pilot

Solve this problem using backward reasoning: start by identifying the exact target quantity, then validate the reasoning path from target back to the given facts.

Rules:
- Do not use any hidden reference answer, answer-key information, or label metadata.
- First, identify the exact quantity the question asks for (the target). Not a related or nearby quantity — the exact one.
- Work backward: starting from the target, trace which operations and facts are required to reach it.
- Check each step for consistency with the target, not with a tempting nearby quantity.
- Review each existing candidate and state whether it matches the identified target.
- `final_answer` must be a bare integer or decimal number. No `$`, `%`, commas, or units. No string wrapping.

QUESTION:
{{question}}

EXISTING CANDIDATES (already generated by prior branches — review whether any match the target):
{{candidate_pool_summary}}

Output a single valid JSON object with exactly these fields and no other text:
{
  "target_identified": "<the exact quantity the question asks for, in plain English>",
  "target_unit": "<unit or type of the answer, e.g. dollars, items, minutes, percent>",
  "backward_check_steps": [
    {
      "step": <step number starting at 1>,
      "description": "<one sentence: what this step computes or validates>",
      "consistent_with_target": <true or false>
    }
  ],
  "candidate_pool_review": "<one sentence: does any existing candidate match the target? state which value, or 'none'>",
  "final_answer": <bare integer or decimal, not a string>
}
