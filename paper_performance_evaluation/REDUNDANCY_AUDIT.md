# Redundancy Audit (Pass 4)

## One-Disclaimer Consolidation

Pass 4 consolidated repeated caveats into the sections where they matter most.

## Consolidated or Removed Repetition

- "Not a new algorithm" language was consolidated into the Introduction's "Measurement objects"
  paragraph. The Method section now simply calls Frontier, FTA, Pooled-4, and adapters controls in
  tables.
- FTA/Pooled-4 caveats were consolidated around identical-pool attribution in
  Section 7. Broad "not every selector fails" repetition was removed from Section 7 and the
  provider-analysis table caption.
- Adapter win-count language was reduced: Table 1 remains a landscape table, and Section 6 now
  describes Class A counts as workload characterization rather than a corrected leaderboard.
- Same-model SC caveats were concentrated in the Azure x GSM8K supporting-control subsection and
  Limitations.
- Lower-bound token/USD language was reduced in the Introduction and captions; detailed semantics
  remain in Compute Accounting, with scope summarized in Limitations.
- Discussion no longer ends with a broad list of non-claims; it states the positive reporting
  implication.

## Retained Qualifications

The following qualifications remain because removing them would change scientific meaning:

- single budget `B=6`;
- 15 completed cells plus one protocol-blocked cell;
- closed-API adapter fidelity limits;
- development adjacency for Cohere x GSM8K FIX-2/FIX-4;
- incomplete token/USD and paid-retry telemetry;
- no FLOP/resource-matched matrix;
- deterministic replay versus proprietary-output regeneration limits;
- historical diagnostics are non-current-path evidence.

## Remaining Redundancy Risks for Pass 5

- Some appendix caveats intentionally restate main-text scope for standalone interpretability.
- Dense table captions still contain local scope language; these can be tightened during layout and
  submission packaging.
- `main.tex` and `title_page.tex` duplicate title/abstract by design for the current Elsevier
  scaffold; author-confirmation fields remain for a later pass.
